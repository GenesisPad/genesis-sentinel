{
  "name": "@genesis-sentinel/providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@genesis-sentinel/chain-adapters": "workspace:*",
    "@genesis-sentinel/security-engine": "workspace:*",
    "@genesis-sentinel/shared": "workspace:*",
    "viem": "2.55.0"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
